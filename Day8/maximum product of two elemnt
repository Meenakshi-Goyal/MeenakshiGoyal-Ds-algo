class Solution {
    public int maxProduct(int[] nums) {
        int n = nums.length;
        Arrays.sort(nums);
        int a = nums[n-2];
        int b = nums[n-1];
        return ((a-1)*(b-1));
    }
}
